// Setup
var collection = {
  2548: {
    album: "Slippery When Wet",
    artist: "Bon Jovi",
    tracks: [
      "Let It Rock",
      "You Give Love a Bad Name"
    ]
  },
  2468: {
    album: "1999",
    artist: "Prince",
    tracks: [
      "1999",
      "Little Red Corvette"
    ]
  },
  1245: {
    artist: "Robert Palmer",
    tracks: [ ]
  },
  5439: {
    album: "ABBA Gold"
  }
};

// Only change code below this line
function updateRecords(id, prop, value) {
  var trackExists = collection.hasOwnProperty["tracks"];
  var accessObject= collection[id];
  var tracks;
  //console.log(accessObject);

  if(value === ""){
    delete collection[prop];
  } else if(prop == "tracks" || value !== ""){
    if(trackExists == false){
      trackArray = [] //create property- with array value
      accessObject.tracks = trackArray.push(value)//create new track array for created property above
      console.log("write");
    }else if(trackExists == true){
      return collection;
    } 
  }
  //console.log("Write")
  //console.log(accessObject);
  console.log(collection);
  return collection;
}

updateRecords(5439, "artist", "ABBA");
